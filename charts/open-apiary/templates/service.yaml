apiVersion: v1
kind: Service
metadata:
  name: {{ include "open-apiary.fullname" . }}
  labels:
    {{- include "open-apiary.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "open-apiary.selectorLabels" . | nindent 4 }}
