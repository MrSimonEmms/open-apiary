apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "open-apiary.fullname" . }}-test-connection"
  labels:
{{ include "open-apiary.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "open-apiary.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
